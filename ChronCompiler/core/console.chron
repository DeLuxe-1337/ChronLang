include core.std

["return"]
!ReadLine :: ?

C `int printf (const char* format, ...);`

import {
    printf :: (string)
    printf :: (string, string)
}

Print :: (message) {
    printf("%s", ToString(message))
}

PrintLn :: (message) {
    printf("%s\n", ToString(message))
}